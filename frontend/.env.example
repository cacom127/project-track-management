VITE_API_BASE_URL=http://localhost:8000
# Cognito User Pool đã deploy (CHANGE-006) — lấy giá trị từ AWS Console
# (Cognito > User pools > project-track-users) hoặc CDK Outputs
# (UserPoolId/UserPoolClientId) sau khi `cdk deploy`. Local dev dùng
# chung 1 User Pool thật với production — Cognito không chạy local được.
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
